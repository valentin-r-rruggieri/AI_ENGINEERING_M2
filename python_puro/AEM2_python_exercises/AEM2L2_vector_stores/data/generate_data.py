"""Regenera el corpus local de la lecture 2."""
from pathlib import Path
root = Path(__file__).parent
# El texto contiene temas distinguibles para probar ranking y filtros por departamento.
(root / "corpus.txt").write_text("El departamento de RR.HH. administra vacaciones, licencias y perfiles de empleados. Las vacaciones se aprueban mediante un líder y las licencias requieren documentación verificable. Finanzas administra facturación, pagos, presupuestos y reportes contables mensuales. Tecnología opera autenticación, integraciones, conectividad, monitoreo y auditoría. Las consultas deben recuperar documentos por similitud y después filtrar por departamento cuando la política lo exige. La base vectorial conserva el texto original, un identificador único, el embedding y metadata de procedencia. Un índice exacto entrega un baseline confiable y un índice aproximado reduce latencia cuando el corpus crece. El equipo registra modelo, dimensión, métrica, fecha de indexación y parámetros del experimento. Los filtros de metadata evitan mostrar información de Finanzas en consultas autorizadas solo para RR.HH. Los benchmarks comparan Recall@K, p50, p95, tiempo de construcción y memoria. Los resultados no se aceptan por intuición: cada cambio de modelo, normalización, HNSW o IVF exige una medición registrada y consultas representativas.", encoding="utf-8")
print("Datos generados.")
